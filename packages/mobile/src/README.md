# Mobile App Source Code

## Structure

- `navigation/` - React Navigation setup
- `screens/` - Screen components (auth, influencer, brand)
- `components/` - Reusable UI components
- `services/` - API clients, storage, notifications
- `hooks/` - Custom React hooks
- `store/` - Zustand state management
- `utils/` - Helper functions
- `constants/` - App constants (colors, sizes, API URLs)
- `types/` - TypeScript type definitions
- `assets/` - Images, icons, fonts

## Next Steps

1. Implement authentication screens
2. Build navigation structure
3. Create API service layer
4. Set up state management
5. Build dashboard screens
